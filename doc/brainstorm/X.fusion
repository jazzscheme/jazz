(module org.jazzscheme.X

(in org.jazzscheme

(class X extends Y implements (I J) uses (jazz jazz.process jazz.util)
  
  (method (foo)
    (put (new Process))
    (let ((fact (new Factory)))
      (put fact))))))
