#!/bin/sh

DIRECTORY=$(dirname "$0")

GSC=$DIRECTORY/gambit/bin/gsc
JAM=$DIRECTORY/kernel/jam.scm

if [ ! -f "$GSC" ]; then
	jbs
fi

if [ ! -f "$GSC" ]; then
	echo "Unable to find ./gambit/bin/gsc."
	exit 1
fi

exec "$GSC" -i "$JAM" "$@"
