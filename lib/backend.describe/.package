(package backend.describe
  
  (title "Describe Backend")
  (description "Describe backend")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (backend.describe (update backend.describe)
                              (dependencies backend)))
  (project ".project"))
