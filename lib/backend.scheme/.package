(package backend.scheme
  
  (root "src")
  (products (backend.scheme (update backend.scheme
                                    backend.scheme.runtime)
                            (dependencies backend)))
  (project ".project"))
