(package backend.scheme
  
  (root "src")
  (products (backend.scheme (update backend.scheme.emit.base
                                    backend.scheme.emit.scheme
                                    backend.scheme.emit.jazz)
                            (dependencies core)))
  (project ".project"))
