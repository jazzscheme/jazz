(package backend
  
  (root "src")
  (products (backend (update backend.describe
                             backend.runtime
                             backend.syntax)
                     (dependencies jazz)))
  (project ".project"))
