(package backend
  
  (root "src")
  (products (backend (update backend.runtime
                             backend.syntax)
                     (dependencies dialect))
            (backend.describe (update backend.describe)
                              (dependencies jazz)))
  (project ".project"))
