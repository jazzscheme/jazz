(package contrib.aes
  
  (title "AES")
  (description "Message encryption and decryption based on the AES symmetric cipher")
  (authors "Marc Feeley")
  (stage release)
  (root "src")
  (products (contrib.aes (alias aes)
                         (update aes)
                         (dependencies scheme)))
  (project ".project"))
