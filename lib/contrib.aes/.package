(package contrib.aes
  
  (title "AES")
  (description "Message encryption and decryption based on the AES symmetric cipher")
  (authors "Marc Feeley")
  (root "src")
  (products (contrib.aes (update aes)
                         (dependencies scheme))
            (aes (alias contrib.aes)))
  (project ".project"))
