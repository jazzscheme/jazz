(package contrib.base64
  
  (title "Base64")
  (description "Base64 encoding and decoding")
  (authors "Marc Feeley")
  (root "src")
  (products (contrib.base64 (dependencies jazz))
            (base64 (alias contrib.base64)))
  (project ".project"))
