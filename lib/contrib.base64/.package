(package contrib.base64
  
  (title "Base64")
  (description "Base64 encoding and decoding")
  (authors "Marc Feeley")
  (stage release)
  (root "src")
  (products (contrib.base64 (update base64)
                            (dependencies scheme
                                          jazz))
            (base64 (alias contrib.base64)))
  (project ".project"))
