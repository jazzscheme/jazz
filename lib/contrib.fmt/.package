(package contrib.fmt
  
  (title "FMT")
  (description "Combinator Formatting")
  (authors "Alex Shinn")
  (stage release)
  (root "src")
  (products (contrib.fmt (update fmt
                                 fmt.fmt-c
                                 fmt.fmt-js
                                 fmt.let-optionals
                                 fmt.mantissa
                                 fmt.string-ports)
                         (dependencies scheme
                                       jazz))
            (fmt (alias contrib.fmt)))
  (project ".project"))
