(package contrib.fmt
  
  (title "FMT")
  (description "Combinator Formatting")
  (authors "Alex Shinn")
  (root "src")
  (products (contrib.fmt (update fmt
                                 fmt.fmt-c
                                 fmt.fmt-js
                                 fmt.let-optionals
                                 fmt.mantissa
                                 fmt.string-ports)
                         (dependencies scheme))
            (fmt (alias contrib.fmt)))
  (project ".project"))
