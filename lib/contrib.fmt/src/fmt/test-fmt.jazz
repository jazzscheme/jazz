(module fmt.test-fmt scheme


(import (scheme.syntax (phase syntax) (except let-optionals*))
        (fmt)
        (fmt.let-optionals (phase syntax))
        (fmt.test (phase syntax))
        (srfi-1)
        (srfi-13)
        (srfi-26)
        (srfi-69))


(native private jazz:error)
(native private jazz:unspecified)


(include "implementation/test-fmt.scm"))
