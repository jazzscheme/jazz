(package contrib.glfw
  
  (root "src")
  (install glfw.install)
  (products (glfw (alias contrib.glfw))
            (contrib.glfw (unit glfw.product)
                          (update glfw
                                  glfw.install
                                  glfw.product)
                          (dependencies jazz
                                        jazz.foreign)))
  (project ".project"))
