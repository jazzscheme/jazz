(package contrib.irregex
  
  (root "src")
  (products (irregex (alias contrib.irregex))
            (contrib.irregex (update irregex
                                     irregex.implementation.irregex
                                     project.irregex.IrRegex-Manifest)
                             (dependencies jazz
                                           scheme)))
  (project (IrRegex-Manifest (unit project.irregex.IrRegex-Manifest))))
