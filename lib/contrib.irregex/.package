(package contrib.irregex
  
  (title "IrRegex")
  (description "IrRegular Expressions")
  (authors "Alex Shinn")
  (root "src")
  (products (contrib.irregex (update irregex
                                     irregex.implementation.irregex)
                             (dependencies scheme))
            (irregex (alias contrib.irregex)))
  (project ".project"))
