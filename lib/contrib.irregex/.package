(package contrib.irregex
  
  (title "IrRegex")
  (description "IrRegular Expressions")
  (authors "Alex Shinn")
  (stage release)
  (root "src")
  (products (contrib.irregex (alias irregex)
                             (update irregex
                                     irregex.implementation.irregex)
                             (dependencies scheme)))
  (project ".project"))
