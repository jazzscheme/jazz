(package contrib.match
  
  (title "Match")
  (description "Hygienic pattern matcher")
  (authors "Alex Shinn")
  (stage release)
  (root "src")
  (products (contrib.match (test match.validate.run)
                           (update match)
                           (dependencies scheme
                                         jazz))
            (match (alias contrib.match)))
  (project ".project"))
