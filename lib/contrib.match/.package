(package contrib.match
  
  (title "Match")
  (description "Hygienic pattern matcher")
  (authors "Alex Shinn")
  (stage release)
  (root "src")
  (products (contrib.match (alias match)
                           (test match.validate.run)
                           (update match)
                           (dependencies scheme
                                         jazz)))
  (project ".project"))
