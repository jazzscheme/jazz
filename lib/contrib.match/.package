(package contrib.match
  
  (root "src")
  (products (match (test match.validate.run)
                   (update match
                           match.validate.run
                           project.match.Match-Manifest)
                   (dependencies jazz)))
  (project (Match-Manifest (unit project.match.Match-Manifest))))
