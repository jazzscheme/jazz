(package contrib.sqlite
  
  (title "SQLite")
  (description "SQLite database engine")
  (authors "Jussi Piitulainen")
  (root "src")
  (install sqlite.install)
  (products (contrib.sqlite (unit sqlite.product)
                            (update sqlite
                                    sqlite.implementation.resqlite3
                                    sqlite.install
                                    sqlite.product)
                            (dependencies scheme)
                            (test sqlite.validate.run))
            (sqlite (alias contrib.sqlite)))
  (project ".project"))
