(package contrib.statprof
  
  (root "src")
  (products (statprof (alias contrib.statprof))
            (contrib.statprof (update statprof)
                              (dependencies scheme)))
  (project (Statprof-Manifest (module project.Statprof-Manifest))))
