(package contrib.statprof
  
  (title "Statprof")
  (description "Statistical profiler")
  (authors "Guillaume Germain" "Marc Feeley")
  (stage release)
  (root "src")
  (install statprof.install)
  (products (contrib.statprof (update statprof
                                      statprof.install)
                              (dependencies jazz
                                            profiler))
            (statprof (alias contrib.statprof)))
  (project ".project"))
