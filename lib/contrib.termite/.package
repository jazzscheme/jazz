(package contrib.termite
  
  (title "Termite")
  (description "Distributed computing")
  (authors "Guillaume Germain")
  (stage release)
  (root "src")
  (products (contrib.termite (update termite)
                              (dependencies jazz
                                            scheme))
            (termite (alias contrib.termite)))
  (project ".project"))
