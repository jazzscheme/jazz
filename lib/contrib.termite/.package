(package contrib.termite
  
  (title "Termite")
  (description "Distributed computing")
  (authors "Guillaume Germain")
  (stage release)
  (root "src")
  (products (contrib.termite (alias termite)
                             (update termite)
                             (dependencies scheme)))
  (project ".project"))
