(package contrib.termite
  
  (root "src")
  (products (termite (alias contrib.termite))
            (contrib.termite (update termite)
                              (dependencies jazz
                                            scheme)))
  (project ".project"))
