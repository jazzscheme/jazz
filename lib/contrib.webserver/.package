(package contrib.webserver
  
  (title "Webserver")
  (description "Web server")
  (authors "Marc Feeley")
  (stage release)
  (root "src")
  (products (contrib.webserver (alias webserver)
                               (update webserver
                                       webserver.implementation.http)
                               (dependencies scheme)))
  (project ".project"))
