(package contrib.webserver
  
  (title "Webserver")
  (description "Web server")
  (authors "Marc Feeley")
  (stage release)
  (root "src")
  (products (contrib.webserver (update webserver
                                       webserver.implementation.http)
                                 (dependencies jazz
                                               gambit))
            (webserver (alias contrib.webserver)))
  (project ".project"))
