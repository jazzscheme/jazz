(package contrib.xml
  
  (title "XML parser")
  (description "XML to SXML deserializer")
  (authors "Oleg Kiselyov" "Mikael More")
  (stage release)
  (root "src")
  (products (contrib.xml (alias xml)
                         (test xml.test)
                         (update xml)
                         (dependencies scheme)))
  (project ".project"))
