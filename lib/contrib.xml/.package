(package contrib.xml
  
  (title "XML parser")
  (description "XML to SXML deserializer")
  (authors "Oleg Kiselyov" "Mikael More")
  (stage release)
  (root "src")
  (products (contrib.xml (test xml.test)
                         (update xml)
                         (dependencies scheme))
            (xml (alias contrib.xml)))
  (project ".project"))
