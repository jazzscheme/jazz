(package contrib.zlib
  
  (root "src")
  (products (zlib (alias contrib.zlib))
            (contrib.zlib (unit zlib.product)
                          (update zlib
                                  zlib.foreign
                                  zlib.product)
                          (dependencies scheme)))
  (project ".project"))
