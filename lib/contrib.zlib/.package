(package contrib.zlib
  
  (title "zlib")
  (description "Compression and decompression of deflate and gzip formats")
  (authors "Marc Feeley" "Manuel Serrano")
  (root "src")
  (products (contrib.zlib (update zlib)
                          (dependencies scheme))
            (zlib (alias contrib.zlib)))
  (project ".project"))
