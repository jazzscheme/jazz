(package core
  
  (title "Core")
  (description "Jazz core")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (core (update core.unit.build
                          core.unit.runtime)))
  (project ".project"))
