(package dialect
  
  (title "Dialect")
  (description "Module language dialect")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (dialect (update dialect
                             dialect.core
                             dialect.development)
                     (dependencies core
                                   scheme.core)))
  (project ".project"))
