(package dialect
  
  (root "src")
  (products (dialect (update dialect
                             dialect.syntax
                             dialect.runtime
                             dialect.development)
                     (dependencies core)))
  (project ".project"))
