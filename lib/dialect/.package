(package dialect
  
  (root "src")
  (products (dialect (update dialect
                             dialect.syntax
                             dialect.runtime
                             dialect.development
                             dialect.base
                             dialect.base.syntax
                             dialect.base.runtime)
                     (dependencies core)))
  (project ".project"))
