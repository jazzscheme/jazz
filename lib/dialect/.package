(package dialect
  
  (title "Dialect")
  (description "Module language dialect")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (dialect (update dialect.core)
                     (dependencies scheme.core)))
  (project ".project"))
