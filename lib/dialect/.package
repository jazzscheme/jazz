(package dialect
  
  (root "src")
  (products (dialect (update dialect
                             dialect.core
                             dialect.development)
                     (dependencies core)))
  (project ".project"))
