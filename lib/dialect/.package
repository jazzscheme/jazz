(package dialect
  
  (root "src")
  (products (dialect (update dialect
                             dialect.syntax
                             dialect.development)
                     (dependencies core)))
  (project ".project"))
