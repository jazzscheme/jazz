(package foundation.backend.scheme
  
  (root "src")
  (products (foundation.backend.scheme (update foundation.backend.scheme)
                                       (dependencies core)))
  (project ".project"))
