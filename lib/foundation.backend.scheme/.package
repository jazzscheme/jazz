(package foundation.backend.scheme
  
  (root "src")
  (products (foundation.backend.scheme (update foundation.backend.scheme.emit
                                               foundation.backend.scheme.runtime)
                                       (dependencies backend.scheme)))
  (project ".project"))
