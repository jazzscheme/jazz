(package foundation.backend
  
  (root "src")
  (products (foundation.backend (update foundation.backend.describe)
                                (dependencies jazz
                                              backend)))
  (project ".project"))
