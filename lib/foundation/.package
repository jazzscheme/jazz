(package foundation
  
  (root "src")
  (products (foundation (update foundation
                                foundation.dialect)
                        (dependencies dialect)))
  (project ".project"))
