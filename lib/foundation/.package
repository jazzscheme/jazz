(package foundation
  
  (root "src")
  (products (foundation (update foundation
                                foundation.syntax)
                        (dependencies dialect)))
  (project ".project"))
