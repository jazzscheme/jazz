(package gambit.ext
  
  (title "Gambit ext")
  (description "Gambit extensions")
  (authors "Guillaume Cartier" "Marc Feeley")
  (root "src")
  (products (gambit.ext (unit gambit.product)
                        (update gambit.ext
                                gambit.log
                                gambit.log.plot
                                gambit.product
                                gambit.walk)
                        (dependencies scheme)))
  (project ".project"))
