(package gambit.log
  
  (title "Gambit log")
  (description "Gambit log library")
  (authors "Marc Feeley")
  (stage release)
  (root "src")
  (products (gambit.log (unit gambit.log.product)
                        (update gambit.log
                                gambit.log.product)
                        (dependencies jazz)))
  (project ".project"))
