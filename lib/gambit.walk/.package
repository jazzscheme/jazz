(package gambit.walk
  
  (title "Gambit walk")
  (description "Gambit walker")
  (authors "Marc Feeley")
  (stage experimental)
  (root "src")
  (products (gambit.walk (update gambit.walk)
                         (dependencies jazz)))
  (project ".project"))
