(package jazz.application
  
  (title "Application")
  (description "Applications with user interface")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.application (update jazz.application)
                              (dependencies jazz
                                            jazz.process))
            (sample-app (run jazz.application.sample)))
  (project ".project"))
