(package jazz.audio
  
  (title "Audio")
  (description "Audio using the Cricket cross-platform library")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.audio (alias audio)
                        (product jazz.audio.product)
                        (update jazz.audio
                                jazz.audio.product)
                        (dependencies jazz
                                      jazz.foreign)))
  (project ".project"))
