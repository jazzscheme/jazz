(package jazz.audio
  
  (title "Audio")
  (description "Audio using the GStreamer library")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.audio (alias audio)
                        (update jazz.audio)
                        (dependencies jazz
                                      jazz.foreign)))
  (project ".project"))
