(package jazz.backend.scheme
  
  (root "src")
  (products (jazz.backend.scheme (update jazz.backend.scheme.emit
                                         jazz.backend.scheme.runtime)
                                 (dependencies scheme.backend.scheme)))
  (project ".project"))
