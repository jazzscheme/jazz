(package jazz.backend
  
  (title "Backend")
  (description "Jazz backend")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (jazz.backend (update jazz.backend)
                          (dependencies jazz
                                        scheme.backend.scheme
                                        backend.scheme)))
  (project ".project"))
