(package jazz.benchmark
  
  (title "Benchmark")
  (description "Benchmarking utilities")
  (authors "Guillaume Cartier")
  (root "src")
  (products (jazz.benchmark (run jazz.benchmark.run)
                            (update jazz.benchmark)
                            (dependencies jazz)))
  (project ".project"))
