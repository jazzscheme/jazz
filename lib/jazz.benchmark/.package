(package jazz.benchmark
  
  (title "Benchmark")
  (description "Benchmarking")
  (authors "Guillaume Cartier")
  (stage experimental)
  (root "src")
  (products (jazz.benchmark (alias benchmark)
                            (run jazz.benchmark.run)
                            (update jazz.benchmark)
                            (dependencies jazz)))
  (project ".project"))
