(package jazz.build
  
  (title "Build")
  (description "Build")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.build (alias build)
                        (update jazz.product)
                        (dependencies jazz)))
  (project ".project"))
