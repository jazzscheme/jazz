(package jazz.build
  
  (title "Build")
  (description "Build")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.build (update jazz.product)
                        (dependencies jazz))
            (build (alias jazz.build)))
  (project ".project"))
