(package jazz.calendar
  
  (root "src")
  (products (jazz.calendar (update jazz.calendar)
                           (dependencies jazz)))
  (project (Calendar-Manifest (module project.jazz.calendar.Calendar-Manifest))))
