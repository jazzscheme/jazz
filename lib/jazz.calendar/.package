(package jazz.calendar
  
  (root "src")
  (products (jazz.calendar (update jazz.calendar
                                   project.jazz.calendar.Calendar-Manifest)
                           (dependencies jazz)))
  (project (Calendar-Manifest (unit project.jazz.calendar.Calendar-Manifest))))
