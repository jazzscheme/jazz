(package jazz.catalog.ui
  
  (title "Catalog user interface")
  (description "Catalog user interface")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.catalog.ui (update jazz.catalog.browse
                                     jazz.catalog.edit)
                             (dependencies jazz))
            (catalog.ui (alias jazz.catalog.ui)))
  (project ".project"))
