(package jazz.catalog
  
  (root "src")
  (products (jazz.catalog (update jazz.catalog
                                  jazz.catalog.entry)
                          (dependencies jazz)))
  (project ".project"))
