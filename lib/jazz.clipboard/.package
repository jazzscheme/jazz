(package jazz.clipboard
  
  (title "Clipboard")
  (description "Clipboard access")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.clipboard (update jazz.clipboard)
                            (dependencies jazz))
            (clipboard (alias jazz.clipboard)))
  (project ".project"))
