(package jazz.columnar
  
  (title "Columnar")
  (description "Column display")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.columnar (update jazz.columnar
                                   jazz.columnar.designer)
                           (dependencies jazz))
            (columnar (alias jazz.columnar)))
  (project ".project"))
