(package jazz.columnar
  
  (title "Columnar")
  (description "Column display")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.columnar (alias columnar)
                           (update jazz.columnar
                                   jazz.columnar.designer)
                           (dependencies jazz)))
  (project ".project"))
