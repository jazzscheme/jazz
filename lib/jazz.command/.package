(package jazz.command
  
  (title "Command")
  (description "Command view (minibuffer)")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.command (alias command)
                          (update jazz.command)
                          (dependencies jazz
                                        jazz.ui)))
  (project ".project"))
