(package jazz.command
  
  (title "Command")
  (description "Command view (minibuffer)")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.command (update jazz.command)
                          (dependencies jazz
                                        jazz.ui))
            (command (alias jazz.command)))
  (project ".project"))
