(package jazz.core
  
  (title "Core")
  (description "Jazz core implementation")
  (authors "Guillaume Cartier")
  (root "src")
  (products (jazz.core (update jazz.core)
                       (dependencies scheme.core)))
  (project ".project"))
