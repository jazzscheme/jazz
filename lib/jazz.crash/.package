(package jazz.crash
  
  (root "src")
  (products (jazz.crash (unit jazz.crash.product)
                        (update jazz.crash
                                jazz.crash.product)
                        (dependencies jazz)))
  (project ".project"))
