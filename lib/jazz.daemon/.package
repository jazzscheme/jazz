(package jazz.daemon
  
  (title "Daemon")
  (description "Operating system daemon")
  (authors "Guillaume Cartier")
  (stage experimental)
  (root "src")
  (products (jazz.daemon (update jazz.daemon)
                         (dependencies jazz
                                       jazz.platform))
            (daemon (alias jazz.daemon)))
  (project ".project"))
