(package jazz.daemon
  
  (title "Daemon")
  (description "Operating system daemon")
  (authors "Guillaume Cartier")
  (stage experimental)
  (root "src")
  (products (jazz.daemon (alias daemon)
                         (update jazz.daemon)
                         (dependencies jazz
                                       jazz.platform)))
  (project ".project"))
