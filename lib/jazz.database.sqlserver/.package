(package jazz.database.sqlserver
  
  (title "SQLServer")
  (description "Interface to the SQLServer database")
  (authors "Guillaume Cartier")
  (stage outdated)
  (root "src")
  (products (jazz.database.sqlserver (update jazz.database.sqlserver
                                             jazz.database.sqlserver.designer)
                                     (dependencies jazz
                                                   jazz.database)))
  (project ".project"))
