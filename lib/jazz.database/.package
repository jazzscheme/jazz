(package jazz.database
  
  (root "src")
  (products (jazz.database (update jazz.database
                                   jazz.database.install
                                   jazz.database.literals
                                   project.jazz.database.Database-Manifest)
                           (dependencies jazz)))
  (project (Database-Manifest (unit project.jazz.database.Database-Manifest))))
