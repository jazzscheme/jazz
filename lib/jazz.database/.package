(package jazz.database
  
  (title "Database")
  (description "Abstract database interface")
  (authors "Guillaume Cartier")
  (stage outdated)
  (root "src")
  (products (jazz.database (update jazz.database
                                   jazz.database.install
                                   jazz.database.literals
                                   jazz.database.odbc
                                   jazz.database.postgre)
                           (dependencies jazz))
            (database (alias jazz.database)))
  (project ".project"))
