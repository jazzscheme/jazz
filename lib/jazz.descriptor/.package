(package jazz.descriptor
  
  (title "Descriptor")
  (description "Component descriptors")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.descriptor (update jazz.descriptor)
                             (dependencies jazz))
            (descriptor (alias jazz.descriptor)))
  (project ".project"))
