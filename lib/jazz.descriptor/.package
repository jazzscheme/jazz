(package jazz.descriptor
  
  (title "Descriptor")
  (description "Component descriptors")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.descriptor (alias descriptor)
                             (update jazz.descriptor)
                             (dependencies jazz)))
  (project ".project"))
