(package jazz.detprof
  
  (title "Detprof")
  (description "Deterministic profiler")
  (authors "Guillaume Cartier")
  (root "src")
  (install jazz.detprof.install)
  (products (jazz.detprof (update jazz.detprof
                                  jazz.detprof.install)
                          (dependencies jazz)))
  (project ".project"))
