(package jazz.digest
  
  (title "Digest")
  (description "Digest")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (install jazz.digest.install)
  (products (jazz.digest (alias digest)
                         (update jazz.digest
                                 jazz.digest.install)
                         (dependencies jazz)))
  (project ".project"))
