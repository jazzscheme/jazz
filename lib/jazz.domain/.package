(package jazz.domain
  
  (title "Domain")
  (description "Domains")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.domain (update jazz.domain)
                         (dependencies jazz
                                       jazz.time))
            (domain (alias jazz.domain)))
  (project ".project"))
