(package jazz.entity
  
  (title "Entity")
  (description "Entities")
  (authors "Guillaume Cartier")
  (stage outdated)
  (root "src")
  (products (jazz.entity (update jazz.entity)
                         (dependencies jazz))
            (entity (alias jazz.entity)))
  (project ".project"))
