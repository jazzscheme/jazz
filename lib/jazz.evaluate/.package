(package jazz.evaluate
  
  (title "Evaluate")
  (description "Evaluation")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.evaluate (update jazz.evaluate)
                           (dependencies jazz))
            (evaluate (alias jazz.evaluate)))
  (project ".project"))
