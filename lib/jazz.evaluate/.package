(package jazz.evaluate
  
  (title "Evaluate")
  (description "Evaluation")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.evaluate (alias evaluate)
                           (update jazz.evaluate)
                           (dependencies jazz)))
  (project ".project"))
