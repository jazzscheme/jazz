(package jazz.event
  
  (title "Event")
  (description "Events")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.event (update jazz.event)
                        (dependencies jazz))
            (event (alias jazz.event)))
  (project ".project"))
