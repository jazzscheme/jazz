(package jazz.event
  
  (title "Event")
  (description "Events")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (install jazz.event.install)
  (products (jazz.event (alias event)
                        (update jazz.event
                                jazz.event.install)
                        (dependencies jazz
                                      jazz.platform)))
  (project ".project"))
