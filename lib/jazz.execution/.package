(package jazz.execution
  
  (title "Execution")
  (description "Execution user-level")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.execution (update jazz.execution)
                            (dependencies jazz))
            (execution (alias jazz.execution)))
  (project ".project"))
