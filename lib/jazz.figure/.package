(package jazz.figure
  
  (title "Figure")
  (description "Figures")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.figure (update jazz.figure)
                         (dependencies jazz))
            (figure (alias jazz.figure)))
  (project ".project"))
