(package jazz.fontconfig
  
  (title "Fontconfig")
  (description "Interface to the Fontconfig font access library")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.fontconfig (test jazz.fontconfig.test)
                             (unit jazz.fontconfig.product)
                             (update jazz.fontconfig
                                     jazz.fontconfig.product)
                             (dependencies jazz))
            (fontconfig (alias jazz.fontconfig)))
  (project ".project"))
