(package jazz.fonts
  
  (title "Fonts")
  (description "Collection of free fonts")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.fonts (alias fonts)
                        (product jazz.fonts.product)
                        (dependencies jazz)))
  (project ".project"))
