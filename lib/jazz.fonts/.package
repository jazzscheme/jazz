(package jazz.fonts
  
  (title "Fonts")
  (description "Collection of free fonts")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.fonts (unit jazz.fonts.product)
                        (dependencies jazz))
            (fonts (alias jazz.fonts)))
  (project ".project"))
