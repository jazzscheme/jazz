(package jazz.fonts
  
  (root "src")
  (products (fonts (alias jazz.fonts))
            (jazz.fonts (unit jazz.fonts.product)
                        (update jazz.fonts.product)
                        (dependencies jazz)))
  (project ".project"))
