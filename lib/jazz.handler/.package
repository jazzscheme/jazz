(package jazz.handler
  
  (title "Handler")
  (description "Event handlers")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (install jazz.handler.install)
  (products (jazz.handler (alias handler)
                          (update jazz.handler
                                  jazz.handler.install)
                          (dependencies jazz
                                        jazz.event
                                        jazz.reference)))
  (project ".project"))
