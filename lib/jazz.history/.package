(package jazz.history
  
  (title "History")
  (description "History navigation")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.history (alias history)
                          (update jazz.history)
                          (dependencies jazz)))
  (project ".project"))
