(package jazz.history
  
  (title "History")
  (description "History navigation")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.history (update jazz.history)
                          (dependencies jazz))
            (history (alias jazz.history)))
  (project ".project"))
