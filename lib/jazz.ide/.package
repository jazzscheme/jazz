(package jazz.ide
  
  (root "src")
  (products (jazz.ide (update jazz.ide
                              jazz.ide.login
                              project.jazz.ide.IDE-Manifest)
                      (dependencies jazz)))
  (project (IDE-Manifest (unit project.jazz.ide.IDE-Manifest))))
