(package jazz.json
  
  (title "JSON")
  (description "JSON readtable-based reader")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.json (alias json)
                       (update jazz.json)
                       (dependencies jazz)))
  (project ".project"))
