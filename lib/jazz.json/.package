(package jazz.json
  
  (title "JSON")
  (description "JSON readtable-based reader")
  (authors "Guillaume Cartier")
  (root "src")
  (products (jazz.json (update jazz.json)
                       (dependencies jazz)))
  (project ".project"))
