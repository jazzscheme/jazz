(package jazz.layout
  
  (title "Layout")
  (description "Layouts")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.layout (alias layout)
                         (update jazz.layout)
                         (dependencies jazz)))
  (project ".project"))
