(package jazz.layout
  
  (title "Layout")
  (description "Layouts")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.layout (update jazz.layout)
                         (dependencies jazz))
            (layout (alias jazz.layout)))
  (project ".project"))
