(package jazz.locale
  
  (title "Locale")
  (description "Localization")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (install jazz.locale.install)
  (products (jazz.locale (update jazz.locale
                                 jazz.locale.components
                                 jazz.locale.install
                                 jazz.locale.literals
                                 jazz.locale.ui)
                         (dependencies jazz)))
  (project ".project"))
