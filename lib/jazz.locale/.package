(package jazz.locale
  
  (root "src")
  (products (jazz.locale (update jazz.locale
                                 jazz.locale.components
                                 jazz.locale.ui)
                         (dependencies jazz)))
  (project ".project"))
