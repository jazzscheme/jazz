(package jazz.match
  
  (root "src")
  (products (match (test jazz.match.validate.run)
                   (update jazz.match
                           jazz.match.validate.run)
                   (dependencies jazz)))
  (project (Match-Manifest (module project.jazz.match.Match-Manifest))))
