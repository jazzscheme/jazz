(package jazz.match
  
  (title "Match")
  (description "Pattern matching")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.match (update jazz.match)
                        (dependencies jazz)))
  (project ".project"))
