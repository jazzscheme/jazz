(package jazz.math
  
  (title "Math")
  (description "Math")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.math (alias math)
                       (update jazz.geometry
                               jazz.math)
                       (dependencies jazz)))
  (project ".project"))
