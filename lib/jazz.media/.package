(package jazz.media
  
  (title "Media")
  (description "Platform media access")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.media (update jazz.media)
                        (dependencies jazz))
            (media (alias jazz.media)))
  (project ".project"))
