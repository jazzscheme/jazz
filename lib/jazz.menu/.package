(package jazz.menu
  
  (title "Menu")
  (description "Menus")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.menu (update jazz.menu)
                       (dependencies jazz))
            (menu (alias jazz.menu)))
  (project ".project"))
