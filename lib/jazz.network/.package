(package jazz.network
  
  (root "src")
  (install jazz.network.install)
  (products (jazz.network (update jazz.network
                                  jazz.network.install
                                  jazz.network.literals
                                  jazz.network.http
                                  project.jazz.network.Network-Manifest)
                          (dependencies jazz)))
  (project (Network-Manifest (unit project.jazz.network.Network-Manifest))))
