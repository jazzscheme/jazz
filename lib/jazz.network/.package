(package jazz.network
  
  (root "src")
  (products (jazz.network (update jazz.network
                                  jazz.network.http)
                          (dependencies jazz.platform)))
  (project (Network-Manifest (module project.jazz.network.Network-Manifest))))
