(package jazz.network
  
  (title "Network")
  (description "Network primitives")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.network (alias network)
                          (update jazz.network)
                          (dependencies jazz)))
  (project ".project"))
