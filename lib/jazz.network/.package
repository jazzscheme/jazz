(package jazz.network
  
  (title "Network")
  (description "Network primitives")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.network (update jazz.network)
                          (dependencies jazz))
            (network (alias jazz.network)))
  (project ".project"))
