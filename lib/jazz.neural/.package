(package jazz.neural
  
  (title "Neural")
  (description "Neural Networks")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.neural (alias neural)
                         (update jazz.neural)
                         (dependencies jazz)))
  (project ".project"))
