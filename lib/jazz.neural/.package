(package jazz.neural
  
  (title "Neural")
  (description "Neural Networks")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.neural (alias neural)
                         (product jazz.neural.product)
                         (update jazz.neural)
                         (dependencies jazz)))
  (project ".project"))
