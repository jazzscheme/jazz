(package jazz.openssl
  
  (title "OpenSSL")
  (description "Interface to the OpenSSL library")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.openssl (alias openssl)
                          (remote (silicon "togethersphere.com/jazz-openssl-silicon"))
                          (product jazz.openssl.product)
                          (update jazz.openssl.product)
                          (dependencies jazz)))
  (project ".project"))
