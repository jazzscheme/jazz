(package jazz.optimize
  
  (title "Optimize")
  (description "Optimize to C code")
  (authors "Guillaume Cartier")
  (stage experimental)
  (root "src")
  (products (jazz.optimize (alias optimize)
                           (update jazz.optimize)
                           (dependencies fmt jazz)))
  (project ".project"))
