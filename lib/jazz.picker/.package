(package jazz.picker
  
  (title "Picker")
  (description "View picker")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.picker (update jazz.picker)
                         (dependencies jazz))
            (picker (alias jazz.picker)))
  (project ".project"))
