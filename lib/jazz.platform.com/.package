(package jazz.platform.com
  
  (title "COM")
  (description "Windows Component Object Model")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.platform.com (update (cond (windows jazz.platform.com)))
                               (dependencies jazz))
            (com (alias jazz.platform.com)))
  (project ".project"))
