(package jazz.print
  
  (title "Print")
  (description "Printing")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.print (alias print)
                        (update jazz.print)
                        (dependencies jazz)))
  (project ".project"))
