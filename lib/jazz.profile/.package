(package jazz.profile
  
  (root "src")
  (products (jazz.profile (update jazz.profile)
                          (dependencies jazz.platform)))
  (project (Profile-Manifest (module project.jazz.profile.Profile-Manifest))))
