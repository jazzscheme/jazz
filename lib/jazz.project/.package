(package jazz.project
  
  (root "src")
  (products (jazz.project (update jazz.project
                                  jazz.project.designer)
                          (dependencies jazz)))
  (project ".project"))
