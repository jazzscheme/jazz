(package jazz.python
  
  (title "Python")
  (description "Python readtable-based reader")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.python (alias python)
                         (update jazz.python)
                         (dependencies jazz)))
  (project ".project"))
