(package jazz.reference
  
  (title "Reference")
  (description "References")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (install jazz.reference.install)
  (products (jazz.reference (update jazz.reference
                                    jazz.reference.install)
                            (dependencies jazz))
            (reference (alias jazz.reference)))
  (project ".project"))
