(package jazz.reporter
  
  (title "Reporter")
  (description "Progress reporting")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.reporter (update jazz.reporter)
                           (dependencies jazz))
            (reporter (alias jazz.reporter)))
  (project ".project"))
