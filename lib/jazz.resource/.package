(package jazz.resource
  
  (title "Resource")
  (description "Resource literals")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (install jazz.resource.install)
  (products (jazz.resource (alias resource)
                           (update jazz.resource
                                   jazz.resource.install
                                   jazz.resource.literals)
                           (dependencies jazz)))
  (project ".project"))
