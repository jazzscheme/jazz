;;;==========
;;;  Scheme
;;;==========
;;;
;;;; Sample Scheme Code
;;;

(define (f)
  a)

;;;; Setup

(define v
  (list (h) y x))

(define (g x)
  a b e)

;;;; Impl

(begin
  
  (define (h)
    (k))
  
  (define (i)
    
    (define (j)
      
      (define (k)
        c d e))
      
    (define (l)
      )))
