(package jazz.schema
  
  (root "src")
  (install jazz.schema.install)
  (products (jazz.schema (update jazz.schema
                                 jazz.schema.install
                                 jazz.schema.literals
                                 jazz.schema.literals.constructors
                                 jazz.schema.registration)
                         (dependencies jazz.platform)))
  (project (Schema-Manifest (module project.jazz.schema.Schema-Manifest))))
