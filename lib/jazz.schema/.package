(package jazz.schema
  
  (title "Schema")
  (description "Data schemas")
  (authors "Guillaume Cartier")
  (root "src")
  (install jazz.schema.install)
  (products (jazz.schema (update jazz.schema
                                 jazz.schema.designer
                                 jazz.schema.install
                                 jazz.schema.literals)
                         (dependencies jazz)))
  (project ".project"))
