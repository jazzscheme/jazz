(package jazz.search
  
  (title "Search")
  (description "Search")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.search (update jazz.search
                                 jazz.search.designer)
                         (dependencies jazz))
            (search (alias jazz.search)))
  (project ".project"))
