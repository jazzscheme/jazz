(package jazz.search
  
  (title "Search")
  (description "Search")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.search (update jazz.search)
                         (dependencies jazz))
            (search (alias jazz.search)))
  (project ".project"))
