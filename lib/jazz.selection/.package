(package jazz.selection
  
  (title "Selection")
  (description "Selection management")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.selection (update jazz.selection)
                            (dependencies jazz))
            (selection (alias jazz.selection)))
  (project ".project"))
