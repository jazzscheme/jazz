(package jazz.selection
  
  (title "Selection")
  (description "Selection management")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.selection (alias selection)
                            (update jazz.selection)
                            (dependencies jazz)))
  (project ".project"))
