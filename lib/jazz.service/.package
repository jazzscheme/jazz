(package jazz.service
  
  (title "Service")
  (description "Service-based communication")
  (authors "Guillaume Cartier")
  (stage alpha)
  (root "src")
  (products (jazz.service (update jazz.service)
                          (dependencies jazz))
            (service (alias jazz.service)))
  (project ".project"))
