(package jazz.settings
  
  (title "Settings")
  (description "Settings")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.settings (update jazz.settings)
                           (dependencies jazz
                                         jazz.io))
            (settings (alias jazz.settings)))
  (project ".project"))
