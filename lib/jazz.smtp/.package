(package jazz.smtp
  
  (root "src")
  (products (jazz.smtp (update jazz.smtp)
                       (dependencies contrib.base64
                                     jazz
                                     srfi-19)))
  (project ".project"))
