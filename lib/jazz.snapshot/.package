(package jazz.snapshot
  
  (title "Snapshot")
  (description "Crash snapshot generation")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.snapshot (test jazz.snapshot.test)
                           (update jazz.snapshot)
                           (dependencies jazz))
            (snapshot (alias jazz.snapshot)))
  (project ".project"))
