(package jazz.snapshot
  
  (title "Snapshot")
  (description "Crash snapshot generation")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.snapshot (update jazz.snapshot)
                           (dependencies jazz)))
  (project ".project"))
