(package jazz.splash
  
  (title "Splash")
  (description "Splash Window")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.splash (alias splash)
                         (product jazz.splash.product)
                         (update jazz.splash
                                 jazz.splash.product)
                         (dependencies jazz)))
  (project ".project"))
