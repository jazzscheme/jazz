(package jazz.template
  
  (title "Template")
  (description "File and directory templating")
  (authors "Guillaume Cartier")
  (root "src")
  (products (jazz.template (update jazz.template)
                           (dependencies jazz)))
  (project ".project"))
