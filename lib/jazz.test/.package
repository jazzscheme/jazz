(package jazz.test
  
  (title "Jazz Tests")
  (description "Jazz tests")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (jazz.test (alias test)
                       (title "Test")
                       (product jazz.test.product)
                       (update jazz.test)
                       (run jazz.test.run)
                       (dependencies jazz)))
  (project ".project"))
