(package jazz.test
  
  (title "Jazz Tests")
  (description "Jazz tests")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (jazz.test (title "Test")
                       (unit jazz.test.product)
                       (update jazz.test)
                       (run jazz.test.run)
                       (dependencies jazz))
            (test (alias jazz.test)))
  (project ".project"))
