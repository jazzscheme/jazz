(unit test.performance


(require (core.class)
         (core.generic)
         (test.performance.syntax (phase syntax))
         (test.performance.common)
         (test.performance.a)
         (test.performance.b)))
