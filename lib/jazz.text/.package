(package jazz.text
  
  (title "Text")
  (description "Text view")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.text (update jazz.text)
                       (dependencies jazz))
            (text (alias jazz.text)))
  (project ".project"))
