(package jazz.time
  
  (title "Time")
  (description "Time")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (install jazz.time.install)
  (products (jazz.time (update jazz.time
                               jazz.time.install)
                       (dependencies srfi-19
                                     jazz))
            (time (alias jazz.time)))
  (project ".project"))
