(package jazz.time
  
  (title "Time")
  (description "Time")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (install jazz.time.install)
  (products (jazz.time (alias time)
                       (update jazz.time
                               jazz.time.install)
                       (dependencies srfi-19
                                     jazz)))
  (project ".project"))
