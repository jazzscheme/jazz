(package jazz.tracker
  
  (title "Tracker")
  (description "Memory Tracking")
  (authors "Guillaume Cartier")
  (stage beta)
  (root "src")
  (products (jazz.tracker (alias tracker)
                          (update jazz.tracker)
                          (dependencies jazz)))
  (project ".project"))
