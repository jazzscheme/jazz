(package jazz.tree
  
  (title "Tree")
  (description "Tree view")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.tree (update jazz.tree)
                       (dependencies jazz))
            (tree (alias jazz.tree)))
  (project ".project"))
