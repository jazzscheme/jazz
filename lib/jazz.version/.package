(package jazz.version
  
  (title "Version")
  (description "Version")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (install jazz.version.install)
  (products (jazz.version (alias version)
                          (update jazz.version
                                  jazz.version.install)
                          (dependencies jazz)))
  (project ".project"))
