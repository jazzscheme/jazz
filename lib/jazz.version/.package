(package jazz.version
  
  (title "Version")
  (description "Version")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (install jazz.version.install)
  (products (jazz.version (update jazz.version
                                  jazz.version.install)
                          (dependencies jazz))
            (version (alias jazz.version)))
  (project ".project"))
