(package jazz.window
  
  (title "Window")
  (description "Cross-platform windows")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.window (update jazz.window
                                 jazz.window.designer)
                         (dependencies jazz))
            (window (alias jazz.window)))
  (project ".project"))
