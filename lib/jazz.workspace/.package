(package jazz.workspace
  
  (title "Workspace")
  (description "Workspaces")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.workspace (update jazz.workspace
                                    jazz.workspace.designer)
                            (dependencies jazz))
            (workspace (alias jazz.workspace)))
  (project ".project"))
