(package jazz.zlib
  
  (root "src")
  (products (zlib (alias jazz.zlib))
            (jazz.zlib (unit jazz.zlib.product)
                       (update jazz.zlib
                               jazz.zlib.product)
                       (dependencies jazz)))
  (project ".project"))
