(package jazz.zlib
  
  (title "Cairo")
  (description "Interface to the zlib data compression library")
  (authors "Guillaume Cartier")
  (stage release)
  (root "src")
  (products (jazz.zlib (test jazz.zlib.test)
                       (unit jazz.zlib.product)
                       (update jazz.zlib
                               jazz.zlib.product)
                       (dependencies jazz))
            (zlib (alias jazz.zlib)))
  (project ".project"))
