(package jazz.zlib
  
  (title "Cairo")
  (description "Interface to the zlib data compression library")
  (authors "Guillaume Cartier")
  (root "src")
  (products (jazz.zlib (unit jazz.zlib.product)
                       (update jazz.zlib
                               jazz.zlib.product)
                       (dependencies jazz))
            (zlib (alias jazz.zlib)))
  (project ".project"))
