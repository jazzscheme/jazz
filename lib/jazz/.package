(package jazz
  
  (root "src")
  (install jazz.install)
  (products (jazz (module jazz.product) (dependencies scheme))
            (all (module jazz.all) (dependencies platform))
            (test (title "Test")
                  (icon "Jedi")
                  (run jazz.test)))
  (project (Jazz-Manifest (module project.jazz.Jazz-Manifest))))
