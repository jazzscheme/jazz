(library jazz.test.c jazz


(export (jazz.console))
(import (jazz.ui))


(definition public (foo)
  (new View))


(definition public (bar x)
  (let ((y (+ x x)))
    (baz x y)))


(define (baz x y)
  (list x y))


(class C extends View))
