(module jazz.test.scheme scheme


(import (scheme.core (phase syntax))
        (jazz.dialect.classes.foundation))


(define-class Foo jazz.test.scheme:Foo Expression (type source) Object-Class allocate-foo
  ((x get-foo-x set-foo-x)))


(define-method (emit-expression (Foo expression) declaration environment backend)
  'Foo)


(encapsulate-class Foo))
