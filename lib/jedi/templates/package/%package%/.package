(package %package%
  
  (root "src")
  (products (%product% (update %project-unit%.%project%-Manifest)
                       (dependencies jazz)))
  (project (%project%-Manifest (unit %project-unit%.%project%-Manifest))))
