(package %package%
  
  (root "src")
  (products (%product% (title "%product-title%")
                       (run %product-unit%.run)
                       (update %product-unit%
                               %product-unit%.run
                               %project-unit%.%project%-Manifest)
                       (dependencies jazz)))
  (project (%project%-Manifest (unit %project-unit%.%project%-Manifest))))
