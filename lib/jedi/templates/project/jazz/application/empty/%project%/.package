(package %package%
  
  (root "src")
  (products (%product% (title "%product-title%")
                       (run %product-unit%.run)
                       (update %product-unit%
                               %product-unit%.run)
                       (dependencies jazz)))
  (project ".project"))
