(package %package%
  
  (root "src")
  (products (%product% (title "%product-title%")
                       (run %product-unit%.run)
                       (update %product-unit%)
                       (dependencies jazz)))
  (project ".project"))
