(package %package%
  
  (root "src")
  (products (%product% (unit %product-unit%.product)
                       (update %product-unit%.product
                               %project-unit%.%project%-Manifest)
                       (dependencies jazz)))
  (project (%project%-Manifest (unit %project-unit%.%project%-Manifest))))
