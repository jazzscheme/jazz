(package %package%
  
  (root "src")
  (products (%product% (title "%product-title%")
                       (run %product-module%.run)
                       (update %product-module%)
                       (dependencies jazz.platform)))
  (project (%project%-Manifest (module %project-module%.%project%-Manifest))))
