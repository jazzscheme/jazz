(library test.carbon jazz


(import (jazz.platform.carbon)
	(jazz.platform.carbon.carbon-types))


(definition (event-loop)
  (let ((record (EventRecord-make))
	(mask (bitwise-ior updateMask))
    (let loop ()
      (WaitNextEvent (


(definition (test)
  (let ((rect (Rect-make)))
    (Rect-left-set! rect 100)
    (Rect-top-set! rect 100)
    (Rect-bottom-set! rect 500)
    (Rect-right-set! rect 500)
    (let ((win (CreateNewWindow kDocumentWindowClass kWindowStandardDocumentAttributes rect)))
      (ShowWindow win)))))