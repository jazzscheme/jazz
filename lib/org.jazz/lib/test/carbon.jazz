(library test.carbon jazz


(import (jazz.library)
        (jazz.ui.window.platform.carbon))


(definition (test)
  (let* ((window (create-window-handle #f #f #f))
	 (view   (create-HIView-handle #f window #f)))
    (show-window window)
    (show-view view)
    (process-window-messages))))