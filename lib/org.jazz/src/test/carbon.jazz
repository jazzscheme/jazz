(library test.carbon jazz


(import (jazz.library)
        (jazz.ui)
        (jazz.ui.window))


(definition (test)
  (let* ((window (new Window)))
    (set-visible?~ window #t)
    (process-window-messages))))