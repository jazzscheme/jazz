(library jedi jazz


(export (jedi.autoload))
(import (jedi.autoload)
        (jazz.platform)
        (jazz.platform.literals)
        (jazz.system))


(register-app 'jedi
  (lambda ()
    (boot-environment~ (new Jedi) '()))))
