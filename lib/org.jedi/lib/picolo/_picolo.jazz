(library picolo jazz


(export (picolo.autoload))
(import (picolo.autoload)
        (jazz.platform)
        (jazz.platform.literals)
        (jazz.system))


(register-app 'picolo
  (lambda ()
    (boot-environment~ (new Picolo) '()))))
