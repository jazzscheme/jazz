(package profiler
  
  (title "Profiler")
  (description "Profiling code")
  (authors "Guillaume Cartier")
  (root "src")
  (products (profiler (update profiler)
                      (dependencies jazz
                                    scheme)))
  (project ".project"))
