(package profiler
  
  (root "src")
  (products (profiler (update profiler)
                      (dependencies jazz
                                    scheme)))
  (project (Profiler-Manifest (unit project.Profiler-Manifest))))
