(package sample.ldap
  
  (root "src")
  (products (sample.ldap (update ldap.ldap0
                                 ldap.ldap1
                                 ldap.ldap2
                                 ldap.ldap3
                                 ldap.ffi)
                         (dependencies jazz)))
  (profiles (LDAP (module profile.ldap)))
  (project (Ldap-Manifest (module project.sample.ldap.Ldap-Manifest))))
