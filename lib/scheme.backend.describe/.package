(package scheme.backend.describe
  
  (title "Scheme Describe Backend")
  (description "Scheme describe backend")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (scheme.backend.describe (update scheme.backend.describe)
                                     (dependencies jazz
                                                   foundation.backend.describe)))
  (project ".project"))
