(package scheme.backend.scheme
  
  (root "src")
  (products (scheme.backend.scheme (update scheme.backend.scheme
                                           scheme.backend.scheme.runtime)
                                   (dependencies core)))
  (project ".project"))
