(package scheme.backend.scheme
  
  (title "Scheme Scheme Backend")
  (description "Scheme Scheme backend")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (scheme.backend.scheme (update scheme.backend.scheme.emit
                                           scheme.backend.scheme.runtime)
                                   (dependencies foundation.backend.scheme)))
  (project ".project"))
