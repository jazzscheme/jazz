(package scheme.backend
  
  (root "src")
  (products (scheme.backend (update scheme.backend.describe)
                            (dependencies jazz
                                          foundation.backend)))
  (project ".project"))
