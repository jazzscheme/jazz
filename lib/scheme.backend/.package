(package scheme.backend
  
  (title "Scheme Backend")
  (description "Scheme backend")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (scheme.backend (update scheme.backend.describe)
                            (dependencies jazz
                                          foundation.backend)))
  (project ".project"))
