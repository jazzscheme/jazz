(package scheme.core
  
  (title "Scheme Core")
  (description "Scheme core")
  (authors "Guillaume Cartier")
  (stage internal)
  (root "src")
  (products (scheme.core (update scheme.core
                                 scheme.core.kernel)))
  (project ".project"))
