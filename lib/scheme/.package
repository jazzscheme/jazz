(package scheme
  
  (root "src")
  (products (scheme (update scheme
                            scheme.dialect
                            scheme.syntax
                            scheme.syntax-rules)
                    (dependencies foundation)))
  (project ".project"))
