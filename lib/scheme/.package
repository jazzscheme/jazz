(package scheme
  
  (root "src")
  (products (scheme (update scheme)
                    (dependencies core))
            (project.scheme (update project.scheme.Scheme-Manifest)))
  (project (Scheme-Manifest (module project.scheme.Scheme-Manifest))))
