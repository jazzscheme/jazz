(package scheme
  
  (root "src")
  (products (scheme (update scheme
                            scheme.dialect
                            scheme.dialect.syntax
                            scheme.dialect.runtime
                            scheme.syntax-rules)
                    (dependencies dialect)))
  (project ".project"))
