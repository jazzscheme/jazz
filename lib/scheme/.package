(package scheme
  
  (root "src")
  (products (scheme (update scheme
                            scheme.dialect
                            scheme.language.syntax-rules)
                    (dependencies foundation)))
  (project ".project"))
