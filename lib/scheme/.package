(package scheme
  
  (root "src")
  (products (scheme (update scheme
                            scheme.dialect
                            scheme.dialect.syntax
                            scheme.language.runtime
                            scheme.language.syntax-rules)
                    (dependencies foundation)))
  (project ".project"))
