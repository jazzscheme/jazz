(package scheme
  
  (root "src")
  (products (scheme (update scheme
                            scheme.syntax-rules)
                    (dependencies core)))
  (project (Scheme-Manifest (unit project.scheme.Scheme-Manifest))))
