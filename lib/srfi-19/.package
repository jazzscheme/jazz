(package srfi-19
  
  (root "src")
  (products (srfi-19 (update time)
                     (dependencies scheme))
            (project.srfi-19 (update project.time.SRFI19-Manifest)))
  (project (SRFI19-Manifest (module project.time.SRFI19-Manifest))))
