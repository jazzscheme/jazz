(library jazz.application jazz


(export (jazz.application.autoload)
        (jazz.application.platform.windows (cond windows))
        (jazz.application.platform.x11 (cond x11)))
(import (jazz.application.autoload)))
