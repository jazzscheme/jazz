(library test.aaa jazz


@w (
(definition public inline (nneq? x y)
  (not (eq? x y)))


(class A extends Object
  
  
  (slot aa accessors generate)
  
  
  (method public inline (yoo a)
    (list self a a)))))
