(library jazz.test.b jazz

(class X extends Object
  
  (method (initialize self)
    (debug 'initialize self 'X))
  
  (method virtual (print self output detail)
    (print-unreadable self output
      (function (output)
        ))))

(generic public (f (X x)))

(specific (f (X x))
  (list 'X))

(definition (foo x)
  (+ x 10 (bar)))

(definition (bar)
  2)

(class Y extends X
  
  (slot a)
  (slot b initialize 2 accessors generate)
  
  (method (print output detail)
    (print-unreadable self output detail
      (function (output)
        )))
  
  (method (foo)
    self)
  
  (method (test)
    (let ((x (new X))
          (y (new Y)))
      (list x y))))

(class W extends Y))
